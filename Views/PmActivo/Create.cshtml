@model MVClog.Models.PmActivo

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutPanel.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PmActivo</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Fecha)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Fecha)
            @Html.ValidationMessageFor(model => model.Fecha)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Concentracion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Concentracion)
            @Html.ValidationMessageFor(model => model.Concentracion)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ContaminanteID, "Contaminante")
        </div>
        <div class="editor-field">
            @Html.DropDownList("ContaminanteID", String.Empty)
            @Html.ValidationMessageFor(model => model.ContaminanteID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
